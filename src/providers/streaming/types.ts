export interface StreamingAnime {
  id: string;
  title: string;
}

export interface StreamingEpisode {
  id: string;
  number: number;
  title: string;
}

export interface StreamingSource {
  url: string;
  quality: string;
}